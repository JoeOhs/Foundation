# Foundation — Personal Bible Study App

A personal, single-user, fully offline desktop Bible study application. Built with Tauri (Rust + React + SQLite) as a replacement for legacy Bible study software.

## Features

- **Parallel view** — up to 4 translations side by side in synced, resizable columns. Pane 1 is the navigation controller: its book/chapter selectors lead sync group A, and notes always anchor to its location. The toolbar 🔗 Sync menu assigns every other pane to **Synced** (follow Pane 1), **Group B** (a second sync combination with its own leader — available from 3 panes up), or **Solo** (fully independent). Scroll-sync operates within each group; group assignments persist with the layout.
- **Library** — a categorised, searchable catalogue (`🌐 Library`) of public-domain texts: collapsible sections for Bibles, Commentaries, Reference works, Historical works, Church Fathers, Rabbinic literature, Reformation works and your own Imports, with Bibles further grouped by language and the Church Fathers, Rabbinic and Reformation works by series and a filter box that matches on title or language across every section. Sections with more than a few entries start collapsed. Bible translations download on demand from a hand-curated, license-checked list (`src/library.ts`): ASV, Berean Standard Bible, Young's Literal, Darby, Douay-Rheims (with deuterocanon), Geneva 1599, JPS 1917 Tanakh, and several non-English classics. Larger works ship inside the app and install with no network call at all — E. W. Bullinger's Companion Bible Appendixes and marginal notes, Josephus's complete works in Whiston's 1737 translation (31 books under one entry), Fox's Book of Martyrs under a Chapter → named entry table of contents — note the edition: Project Gutenberg's #22400 is a 19th-century compilation and abridgement built on Foxe's work, extended by its unnamed editor to 1830, not a transcription of his original 1563/1570 “Actes and Monuments” — and the complete Church Fathers collection in Schaff's editions — Ante-Nicene Fathers (9 volumes), Nicene and Post-Nicene Fathers Series I (14) and Series II (14), 37 volumes in all, each its own entry with a full table of contents and editorial footnotes excluded. The Babylonian Talmud ships the same way, as six entries — one per Seder — covering all 37 tractates under a Seder → Tractate → Daf table of contents. It is the **one exception** to the Library's public-domain-only rule: the William Davidson Talmud is Rabbi Adin Even-Israel Steinsaltz's English translation of the Koren Noé Talmud (Koren Publishers Jerusalem), underwritten by the William Davidson Foundation and published by Sefaria, licensed **CC BY-NC 4.0** — shareable with attribution, non-commercial use only. It was included deliberately, with the licence disclosed in the Library panel itself, because the only public-domain English Talmud (Rodkinson, 1918) covers roughly a third of the tractates and is of poor repute. The Jerusalem Talmud (Yerushalmi) ships alongside it as a single entry covering all 39 tractates across five Sedarim, under a Seder → Tractate → Chapter:Halakhah table of contents — the Yerushalmi has no standard pagination, so it is cited by chapter and halakhah rather than by daf. It is Heinrich W. Guggenheimer's translation, published in 17 volumes by Walter de Gruyter (Berlin, 1999–2015) and digitised by Sefaria, licensed **CC BY** — attribution only, with none of the non-commercial restriction the Bavli carries, so it is not a second exception to the public-domain rule. It was chosen over the only public-domain English Yerushalmi (Schwab, 1886), which covers Berakhot alone, and over Sefaria's CC0 community translation, which reaches under 1% of the corpus; Guggenheimer's footnoted commentary is omitted. Reformation writings ship as two entries — Volumes I and II of *Works of Martin Luther, with Introductions and Notes* (the Philadelphia Edition, A. J. Holman Company, 1915 and 1916), in which a team of Lutheran scholars translated Luther's major treatises, sermons and open letters directly from the German and Latin, each work carrying its own introduction and named translator. Sixteen treatises in all — among them the XCV Theses, the Open Letter to the Christian Nobility, the Babylonian Captivity of the Church and the Treatise on Christian Liberty — under a Work → Section table of contents, alongside Volume I's own scholarly introduction, with the editors' footnotes excluded and the printed marginal sidenotes kept as entry headings. Note the scope: these are 2 of the edition's 6 volumes, the two Project Gutenberg has digitised as clean text; Volumes III–VI exist only as page scans and are not included. Every other Library text is public domain and individually verified so. No account, no marketplace, no paid content.
- **Import pipeline** — bring in plain text, Markdown, JSON, CSV/TSV, XML, or EPUB. Verse-keyed texts map onto book/chapter/verse; anything else (including EPUB, which is always treated as freeform) is stored as page/section-anchored entries. EPUB imports also capture the book's table of contents into a dedicated `toc_entries` table, pointing at the matching entries, ready for a future dedicated reading pane. Unparseable input degrades gracefully to a freeform document instead of failing. Legacy SQLite-based module files you own can be read as a one-time personal data migration.
- **Highlighters** — label your own colored highlighters and apply them to verses (single or shift+click ranges) from the reader action bar, or to a section of an imported text (click a paragraph in its dedicated pane); highlights persist, show across every translation, and are managed in a Highlights tab in the Notes panel that lists them grouped by color with jump-to-verse/section and add-to-note. Palette is fully editable (rename/recolor/add/delete).
- **Links** — "Bind" two things together across panes: two verses, two imported-text sections, or a verse and a section, in any combination (select one, 🔗 Link, select another, Bind). Bound items show a dashed outline; a Links tab lists every binding with both endpoints, "Loose" to remove, add-to-note, and an optional highlighter-color association. Works across translations, imported texts, and in the popped-out window.
- **Notes** — a Markdown study workspace. Anchor a note to a verse, chapter, book (shown across all translations), or a specific section of an imported text, or leave it freeform. Pin notes (📌) to keep them at the top of the list; two pinned tutorial notes ship on first run — a welcome tour and an advanced Markdown / import-export reference. Notes are written and rendered as Markdown (formatting toolbar with Write/Preview); select verses in the reader (shift+click for a range) or a paragraph in an imported-text pane, and drop them into the open note as a formatted blockquote. Import legacy notes from Markdown, plain text, RTF, or HTML (converted to Markdown on the way in) and export back out to a single Markdown file — the export picker (📤) lists every note grouped by its anchor, with everything pre-selected so "export all" stays one click, plus per-note and per-group checkboxes and a filter box for exporting just a subset. The notes panel can pop out into its own window for a second monitor or side-by-side layout, staying live-synced with the main window over the shared database. Stored locally in SQLite.
- **Themes** — six visual themes (Obsidian, Midnight, Cosmic, Sunset, Emerald, Nova) with per-theme gradient shells and accent systems, defined in [docs/THEMES.md](docs/THEMES.md) and implemented as a CSS-variable registry (`src/themes.css`). OS-aware default (Obsidian on dark systems, Nova on light), instant switching with live hover preview from the 🎨 picker, reading panes always flat for protected text contrast (verified ≥12:1 on every theme), and motion respects `prefers-reduced-motion`.
- **Search** — SQLite FTS5 full-text search across Bible translations and notes, grouped by source, click-to-navigate. Scoped to `sources.type = 'bible'` — commentaries, reference works, and freeform library imports (e.g. EPUB) aren't included.
- **Smart search (KJV + Strong's numbers)** — an optional Library add-on tags the KJV with Strong's numbers; once installed, searching a word groups results by the original Hebrew/Greek word it actually translates (e.g. "love" splits into agapē vs phileō vs chesed), each group showing a gloss and its verse list with the matched word highlighted inline, plus a total occurrence count. Additive — regular full-text search always still runs alongside it. Tagged words in the reader are individually clickable to look up every other occurrence of that same original word.
- **Concordance pane** — the grouped Strong's view is also available as a docked side pane (🔤 in the toolbar) that scrolls independently of the Bible panes, for longer study sessions. With the pane open, clicking a tagged word updates it in place; with it closed, word clicks open the search modal, which has an "Open in pane →" button to promote the lookup. Verse clicks in the pane navigate the reader without closing it.
- **Readability** — adjustable reader font size and a curated reader-font choice (Georgia, Palatino, Cambria, Constantia, Segoe UI, Verdana — system font stacks only, nothing bundled or licensed), with layout/theme/font/reference persistence between sessions.

Seeded with two public-domain translations: King James Version and Bible in Basic English (`public/seed/`). More are available in-app via **Library**.

## License

Foundation's source code is MIT-licensed — see [LICENSE](LICENSE). This is a personal, non-commercial project; there's no intent to monetize it, and contributions are welcome.

The MIT license covers the app itself, not the texts you load into it. Bible translations and other texts you import or download keep whatever license or public-domain status they already have — check the license note shown for each source. The bundled seed and the in-app Library only include texts that have been individually verified as public domain, with one deliberate, disclosed exception — the CC BY-NC 4.0 William Davidson Talmud described above (the CC BY Jerusalem Talmud is attribution-only, not a further exception); see `src/library.ts` for the reasoning behind each entry.

See [ROADMAP.md](ROADMAP.md) for planned work.

## Development

Prerequisites: Node 20+, Rust (stable, MSVC toolchain on Windows).

```
npm install
npm run tauri dev     # run the desktop app in dev mode
npm run tauri build   # produce a distributable build
```

## Architecture

- `src-tauri/` — thin Rust shell: tauri-plugin-sql (SQLite), tauri-plugin-dialog, plus `read_file_text` / `read_file_base64` commands for the importer.
- `src/db.ts` — schema, migrations (FTS5 with LIKE fallback), and all queries. The database lives in the app config directory as `foundation.db`.
- `src/importer.ts` — format sniffing and forgiving parsers for each import format.
- `src/seed.ts` — first-run install of the bundled translations.
- `src/library.ts` — curated manifest of downloadable public-domain translations, shares `src/bibleJsonFormat.ts` conversion with the seeder; also defines the Library's add-ons (currently just KJV + Strong's numbers).
- `src/strongsImport.ts` — dedicated one-time importer for the KJV+Strong's OSIS file and the OpenScriptures dictionaries (not part of the general `importer.ts` sniffing pipeline — this is a fixed, known format). Idempotent: safe to re-run, clears previous Strong's data for the KJV first.
- `src/bullingerRefs.ts` — finds E. W. Bullinger's cross-references (`Eph. 3. 1`, `Ap. 98. XII`, bare `v. 22`) inside a Companion Bible note and resolves them to a verse or an appendix. Pure text analysis: it reports spans, never rewrites the note.
- `src/components/` — Pane (reader column), NotesPanel, SearchPanel, ImportWizard, LibraryPanel, StrongsWords (shared word-tagging/highlighting renderer used by both Pane and SearchPanel), ReferenceText (renders commentary prose with Bullinger's references clickable).
- `public/library/` — bundled reference works shipped inside the app, grouped by category: `patristic/` (the complete Church Fathers collection — Ante-Nicene Fathers vols. 1–9, Nicene and Post-Nicene Fathers Series I vols. 1–14 and Series II vols. 1–14, 37 volumes), `commentaries/` (Companion Bible notes and appendixes, Jamieson-Fausset-Brown), `historical/` (Josephus, Fox's Book of Martyrs), `reformation/` (Luther's works, Philadelphia Edition, Vols. I–II), `rabbinic/` (the William Davidson Talmud, one bundle per Seder; the Jerusalem Talmud in one bundle), `reference/` (Smith's Bible Dictionary). Each `*Import.ts` in `src/` reads its bundle from this path with no network call. `public/seed/` holds the first-run KJV/BBE seed data separately.
- `tools/` — standalone one-time build and scrape scripts, run outside the app with plain `node`, one directory per source: `tools/anf/anf01…anf09/`, `tools/npnf1/npnf101…npnf114/`, `tools/npnf2/npnf201…npnf209/` (which additionally share one parsing module, `tools/npnf2/shared/`, rather than copying it per volume), `tools/josephus/`, `tools/jfb/`, `tools/smiths-dictionary/`, `tools/companion-bible-appendix/`, `tools/companion-bible-notes/`, `tools/talmud/`, `tools/yerushalmi/`. Each compiles its raw source material into the matching bundle under `public/library/`. Nothing in `src/` imports these; they are not part of the app build.
- `docs/` — specs and design notes (`docs/THEMES.md`, `docs/theme-lab.html`). `README.md`, `ROADMAP.md` and `LICENSE` stay at the repo root by convention.

### Data model

`sources` (a translation/commentary/reference work) → `books` (or a single synthetic container for freeform texts) → `entries` (verse-keyed via `chapter`/`verse`, or section-keyed via `position_ref`). `sources.is_verse_keyed` flags which sources are actual Bible translations versus freeform library imports (EPUB and similar). `notes` anchor by canonical reference (`anchor_book`/`anchor_chapter`/`anchor_verse`) so a verse note appears in every translation, or by `entry_id` for imported non-canonical texts, or float free.

A source carries two classifications that look similar but do different jobs, and shouldn't be collapsed into one. `sources.type` is **behavioural**: `type === 'bible'` is what drives pane roles, sync eligibility and search scoping (see `src/sourceRoles.ts`), so changing it moves a source between panes. `sources.category` is **organisational** — it only decides which Library section a source files under (`bible` / `commentary` / `reference` / `historical` / `patristic` / `rabbinic` / `dictionary` / `devotional` / `imported`) — and exists because `type` can't express the distinction: Josephus and an imported EPUB must both be `type: 'extra-biblical'` to behave correctly, yet belong in different sections. The `dictionary` and `devotional` categories route a work to the study footer (`src/components/FooterPanel.tsx`) instead of a reading pane, and file it under the Library's Add-ons section rather than a category section of its own. `sources.language` is an ISO 639-1 code (never a display name — `src/language.ts` owns the code↔name mapping), required on every `bible`-category source since the Library groups Bibles by language.

A **compound work** is one source with many `books` rows beneath it, rather than one source per file — Josephus's complete works (four Gutenberg texts, 31 books), the Companion Bible commentary (one book per transcribed book of the Bible) and each Seder of the Babylonian Talmud and the Jerusalem Talmud (one book per tractate) all use this. It stays a single row in the Library; its internal structure is navigated through the pane's TOC dropdown.

A **footer commentary** likewise adds no tables: one `footer-commentary`-type, `commentary`-category source, one book per Bible book, one entry per comment block. A comment covering a verse range is a single row — `entries.verse` is the first verse it covers and `entries.position_ref` the whole covered range ("5-6"), read back with `versesInRefRange` in `src/scripture.ts` (shared with the Companion Bible's Structure lines). `entries.heading` is a nullable column carrying the source's own section heading over a comment, where it has one (JFB's "Ge 2:2-7. The First Sabbath."). The type is separate from `commentary` because the two behave differently: a `commentary` reads in its own pane, a `footer-commentary` never can — it lives in the study footer's Commentary tab as a horizontal strip of verse cells following Pane 1's chapter, and `isFooterOnly` in `src/sourceRoles.ts` keeps it out of every pane's source picker. Since a verse can be covered by more than one comment (JFB comments on 1 Chr 1:4-23 and again on 1 Chr 1:12), `buildChapterIndex` in `src/components/FooterCommentary.tsx` arranges a chapter's comments into containment order once per chapter load and renders the narrower one nested inside the wider one's cell.

A **dictionary** also maps onto the same model with no tables of its own: one `dictionary`-category source, one book per initial letter, one entry per article with the headword in `entries.position_ref` — `dictionaryLookup` in `src/db.ts` is a case-insensitive prefix query on it. Dictionaries read in the study footer's Dictionary tab (a resizable strip under the panes with the headword list beside its article), never in a pane.

`toc_entries` (`source_id`, `entry_id`, `title`, `level`, `position_ref`, `sort_order`, `chapter`) captures a freeform source's table of contents at import time, one row per heading pointing at the `entries` row it resolves to, with `level` preserving arbitrary nesting depth — EPUB imports produce 1–2 levels, a compound work produces three (Work → Book → Chapter). `insertTocEntries` resolves each row against its own book via `ParsedTocEntry.bookIndex`, so a TOC can span every book of a compound work; a row with `entryIndex: -1` is a grouping heading that labels its children without being jumpable. `book_name` is joined from the target entry rather than stored, because a compound work restarts chapter numbering in each book, so a chapter number alone can't identify where to jump. Rendered as a jump-to dropdown in the dedicated pane.

`highlights` and `links` each anchor either to a canonical verse (`book`/`chapter`/`verse`) or to a specific imported entry (`entry_id`/`entry_id_a`/`entry_id_b`) — exactly one of the two per row (or per endpoint, for `links`), so a highlight/binding works the same way on a Bible verse or a paragraph of an imported EPUB. `links` supports any combination of endpoint kinds (verse↔verse, verse↔entry, entry↔entry).

`strongs_words` tags individual words of an `entries` row with a Strong's number (`entry_id`, `word_index`, `surface_text`, `strongs_number` — one row per tagged word; a word can have more than one number, sharing the same `word_index`). `strongs_dict` holds the lemma/transliteration/gloss for each number. Rendering never reconstructs verse text from `strongs_words` alone — the KJV OSIS source has ~21k translator-supplied words with no Strong's tag at all, so doing that would silently drop words. Instead, tagged spans are aligned onto the existing `entries.text` (see `alignWordsToText` in `src/components/StrongsWords.tsx`), so the visible reading is always exactly the entry text, just partitioned into clickable/highlightable spans plus plain filler wherever a slot can't be matched.

`structure_diagrams` / `structure_lines` / `structure_groups` /
`structure_group_members` hold Bullinger's Structure outlines (see the
Companion Bible entry in [ROADMAP.md](ROADMAP.md)). A diagram anchors to a
verse range on its source; `structure_lines` carries only outline metadata
(`parent_id`, `depth`, `label`, `ref_range`) and points at an ordinary
`entries` row for the line's text — so highlights/links/notes work on a
single outline line through the existing `entry_id` anchor, with no schema of
their own. `structure_lines.entry_id` is null for a "bracket" line (one of
Bullinger's bold letters spanning a block of members without text of its
own), which therefore has nothing to highlight or annotate.
`structure_groups` are the braces down the right margin; a group is often
non-contiguous, since correspondence pairs sit at opposite ends of an
outline. A diagram may carry `reference_pdf_path`/`reference_pdf_page`
pointing at the scanned page it was transcribed from, shown via a
"View original page" link that opens the PDF in its own window
(`?window=refpage`) — supplementary, never required. All four tables cascade
away with their source in `deleteSource`.

`entry_notes` holds translator's notes (alternate readings, literal Hebrew/Greek renderings) captured from OSIS `<note>` elements during the Strong's import — same additive principle as `strongs_words`: the importer only ever INSERTs into its own tables and never writes `entries.text`. Notes anchor after a tagged word via `word_index` (NULL = verse-level) and render as small `°` footnote markers with a hover/click popover. A historical caveat lives in `src/bibleJsonFormat.ts`: the seed JSON overloads `{braces}` for both supplied words (`{was}` — keep) and inline notes (`{firmament: Heb. expansion}` — drop), distinguished by the colon; early builds leaked the note text into `entries.text`, which a one-time boot repair (`repairSeededTextsIfNeeded` in `src/seed.ts`, gated by a `meta` flag) corrects offline from the bundled seed.
